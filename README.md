# ons_postcode_extractor

A command-line Ruby app that extracts, filters, and enriches data from the ONS Postcode Directory (ONSPD) CSV files.

## Usage

```bash
ons_postcode_extractor process path/to/ONSPD.csv
